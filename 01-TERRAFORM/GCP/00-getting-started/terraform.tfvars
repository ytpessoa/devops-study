#To persist variable values

cidrs = [ "10.0.0.0/16", "10.1.0.0/16" ]

/*
project = "<PROJECT_ID>"
credentials_file = "<NAME>.json"

usernames and passwords files
multiple -var-file arguments 
$ terraform apply \
  -var-file="secret.tfvars" \
  -var-file="production.tfvars"

*/

